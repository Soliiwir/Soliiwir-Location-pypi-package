[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "location-package"
version = "0.1.0"
description = "A Python package for managing locations (midterm project)."
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
readme = "README.md"
license = { file = "License" }
requires-python = ">=3.8"
dependencies = []
